import { asResult } from "../interpreter";
import { CRuntime, FunctionCallInstance } from "../rt";
import * as common from "../shared/common";
import { InitIndexPointerVariable, PointeeVariable, PointerVariable, Function, Variable, variables, InitArithmeticVariable, Gen, MaybeUnboundVariable, ResultOrGen, MaybeLeftCV, ObjectType, InitDirectPointerVariable, ClassType, InitValue, AbstractVariable } from "../variables";


export = {
    load(rt: CRuntime) {


    }
};
